# STEP配信システム Ver.30

安定化版です。

## 重要
- Apps Script側は `Code.gs` と `appsscript.json` を更新してください。
- 更新後、`setupStepMailSystem` を一度実行してください。
- その後、新しいバージョンでデプロイしてください。

## Ver.30の主な修正
- 欠席・遅刻は更新ボタンで元シートを直接読みます。
- 送信履歴の本文は代表1通分だけ保存します。
- 生徒一覧はブラウザキャッシュを使い、起動時表示を速くします。
